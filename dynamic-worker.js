import base from './worker.js';
import { renderOpportunityPage } from './seo-page.js';

const BASE = 'https://trytoolscout.org';
const xmlEscape = value => String(value ?? '').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&apos;');
const slugFromPath = pathname => { const match=pathname.match(/^\/([a-z0-9][a-z0-9-]*)\.html$/i); return match ? decodeURIComponent(match[1]) : null; };
async function staticAsset(request,env){ try { const response=await env.ASSETS.fetch(request); return response.ok?response:null; } catch { return null; } }
async function dynamicOpportunity(request,env,slug){ const row=await env.DB.prepare("SELECT intent_slug,search_sessions,commercial_score,catalog_score,duplication_penalty,opportunity_score,status,updated_at FROM seo_opportunities WHERE intent_slug=? LIMIT 1").bind(slug).first(); if(!row||!['ready','published'].includes(String(row.status))) return null; const toolsResponse=await env.ASSETS.fetch(new Request(new URL('/data/tools.json',request.url))); if(!toolsResponse.ok)return null; const html=renderOpportunityPage({slug,opportunity:row,tools:await toolsResponse.json()}); return new Response(html,{status:200,headers:{'Content-Type':'text/html; charset=UTF-8','Cache-Control':'public, max-age=300, s-maxage=3600','X-ToolScout-Source':'dynamic-opportunity'}}); }
async function dynamicSitemap(request,env){ const baseResponse=await staticAsset(new Request(new URL('/sitemap.xml',request.url)),env); const urls=new Set([BASE+'/']); if(baseResponse){const text=await baseResponse.text();for(const match of text.matchAll(/<loc>([^<]+)<\/loc>/g))urls.add(match[1]);} const consolidations=await readSeoConsolidations(request,env); const rows=await env.DB.prepare("SELECT intent_slug FROM seo_opportunities WHERE status IN ('ready','published') ORDER BY opportunity_score DESC LIMIT 500").all(); for(const row of rows.results||[]){const slug=String(row.intent_slug||'');if(/^[a-z0-9][a-z0-9-]*$/i.test(slug)&&!consolidations[slug])urls.add(`${BASE}/${slug}.html`);} const xml=`<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${[...urls].map(url=>`  <url><loc>${xmlEscape(url)}</loc></url>`).join('\n')}\n</urlset>\n`; return new Response(xml,{status:200,headers:{'Content-Type':'application/xml; charset=UTF-8','Cache-Control':'public, max-age=300, s-maxage=3600'}}); }
async function contentSignals(request,env){ const rows=await env.DB.prepare("SELECT intent_slug,COUNT(DISTINCT session_id) AS searches FROM search_events WHERE created_at >= datetime('now','-30 days') AND intent_slug IS NOT NULL AND intent_slug != 'general' GROUP BY intent_slug ORDER BY searches DESC, intent_slug ASC LIMIT 100").all(); return Response.json({ok:true,window_days:30,signals:(rows.results||[]).map(r=>({intent_slug:String(r.intent_slug),searches:Number(r.searches||0)}))},{headers:{'Content-Type':'application/json; charset=UTF-8','Cache-Control':'private, max-age=60'}}); }
async function readAffiliateMap(request,env){ try{const response=await env.ASSETS.fetch(new Request(new URL('/data/affiliate.json',request.url)));if(!response.ok)return {};return await response.json();}catch{return {};}}
async function consolidatedSeoRedirect(request,env,url){if(!/^\/best-[a-z0-9-]+(?:\.html)?$/i.test(url.pathname))return null;try{const response=await env.ASSETS.fetch(new Request(new URL('/data/seo-consolidations.json',request.url)));if(!response.ok)return null;const redirects=await response.json();const slug=url.pathname.slice(1).replace(/\.html$/i,'');const target=redirects[slug];if(!target)return null;return new Response(null,{status:301,headers:{Location:`${BASE}/${target}.html`,'Cache-Control':'public, max-age=3600'}});}catch{return null;}}
async function readSeoConsolidations(request,env){try{const response=await env.ASSETS.fetch(new Request(new URL('/data/seo-consolidations.json',request.url)));return response.ok?await response.json():{};}catch{return {};}}
function rowsOnly(result){return result?.results||[];}
function isOwnerRequest(request){return (request.headers.get('Cookie')||'').split(';').some(part=>part.trim()==='toolscout_owner=1');}
async function isAccessAuthenticated(request,ctx){
  const email=request.headers.get('Cf-Access-Authenticated-User-Email')||request.headers.get('cf-access-authenticated-user-email')||'';
  if(email.toLowerCase()==='pcaiano@gmail.com') return true;
  try{
    if(!ctx?.access) return false;
    const identity=await ctx.access.getIdentity();
    return String(identity?.email||'').toLowerCase()==='pcaiano@gmail.com';
  }catch{return false;}
}
function cors(){return {'Access-Control-Allow-Origin':'*','Access-Control-Allow-Methods':'GET,POST,OPTIONS','Access-Control-Allow-Headers':'Content-Type,Authorization'};}
async function trackedClick(request,env){try{const b=await request.json();const tool=String(b.tool||'').slice(0,100),intent=String(b.intent||'general').slice(0,100),session=String(b.session||'').slice(0,100);let source=String(b.source||'recommendation').slice(0,100);if(!tool||!session)return Response.json({error:'invalid_event'},{status:400,headers:cors()});if(isOwnerRequest(request))source='internal-test';await env.DB.prepare("INSERT INTO click_events (tool_slug,intent_slug,session_id,source,created_at) VALUES (?,?,?,?,datetime('now'))").bind(tool,intent,session,source).run();return Response.json({ok:true},{headers:cors()});}catch{return Response.json({error:'invalid_request'},{status:400,headers:cors()});}}
async function trackedSearch(request,env){try{const b=await request.json();const intent=String(b.intent||'general').slice(0,100),session=String(b.session||'').slice(0,100),profile=JSON.stringify(b.profile||{}).slice(0,2000);let source=String(b.source||'search').slice(0,100);if(!session)return Response.json({error:'invalid_event'},{status:400,headers:cors()});if(isOwnerRequest(request))source='internal-test';await env.DB.prepare("INSERT INTO search_events (intent_slug,profile_json,session_id,source,created_at) VALUES (?,?,?,?,datetime('now'))").bind(intent,profile,session,source).run();return Response.json({ok:true},{headers:cors()});}catch{return Response.json({error:'invalid_event'},{status:400,headers:cors()});}}
async function trackedRedirect(request,env,tool){try{const config=await (await env.ASSETS.fetch(new Request(new URL('/data/affiliate.json',request.url)))).json();const entry=config[tool];const referrer=request.headers.get('Referer')||request.headers.get('Referrer')||'';let seoIntent='general';try{const refUrl=new URL(referrer);const page=refUrl.pathname.split('/').filter(Boolean).pop()?.replace(/\.html$/i,'')||'';if(page&&page!=='seo')seoIntent=page.slice(0,100);}catch{} const owner=isOwnerRequest(request);const source=owner?'internal-test':(seoIntent!=='general'?'seo-page':'affiliate-redirect');const cookie=request.headers.get('Cookie')||'';const match=cookie.match(/(?:^|;\s*)toolscout_session=([^;]+)/);const session=match?.[1]||crypto.randomUUID();const destination=entry?.enabled&&entry.url?entry.url:entry?.publicUrl;if(!destination)return null;await env.DB.prepare("INSERT INTO click_events (tool_slug,intent_slug,session_id,source,created_at) VALUES (?,?,?,?,datetime('now'))").bind(tool,seoIntent,session,source).run();const headers=new Headers();headers.set('Location',destination);if(!match)headers.append('Set-Cookie',`toolscout_session=${session}; Max-Age=15552000; Path=/; SameSite=Lax`);return new Response(null,{status:302,headers});}catch{return null;}}
async function stats(request,env,ctx){const url=new URL(request.url);const token=(request.headers.get('Authorization')||'').replace(/^Bearer\s+/i,'');const isPublicHost=url.hostname===new URL(BASE).hostname;const legacyAuthorized=!isPublicHost&&!!env.ADMIN_TOKEN&&token===env.ADMIN_TOKEN;const accessAuthorized=isPublicHost&&await isAccessAuthenticated(request,ctx);if((isPublicHost&&!accessAuthorized)||(!isPublicHost&&!legacyAuthorized))return Response.json({error:'unauthorized'},{status:401});const [byTool,byIntent,bySource,bySearchSource,total,searches,opportunities,dailyClicks,dailySearches,affiliate]=await Promise.all([env.DB.prepare('SELECT tool_slug,COUNT(*) AS clicks FROM click_events GROUP BY tool_slug ORDER BY clicks DESC LIMIT 20').all(),env.DB.prepare('SELECT intent_slug,COUNT(*) AS clicks FROM click_events GROUP BY intent_slug ORDER BY clicks DESC LIMIT 20').all(),env.DB.prepare("SELECT COALESCE(NULLIF(source,''),'direct') AS source,COUNT(*) AS clicks FROM click_events GROUP BY COALESCE(NULLIF(source,''),'direct') ORDER BY clicks DESC LIMIT 20").all(),env.DB.prepare("SELECT COALESCE(NULLIF(source,''),'direct') AS source,COUNT(DISTINCT session_id) AS searches FROM search_events WHERE created_at >= datetime('now','-30 days') GROUP BY COALESCE(NULLIF(source,''),'direct') ORDER BY searches DESC LIMIT 20").all(),env.DB.prepare('SELECT COUNT(*) AS clicks,COUNT(DISTINCT session_id) AS sessions FROM click_events').first(),env.DB.prepare('SELECT intent_slug,COUNT(DISTINCT session_id) AS searches FROM search_events GROUP BY intent_slug ORDER BY searches DESC LIMIT 20').all(),env.DB.prepare('SELECT intent_slug,search_sessions,commercial_score,catalog_score,duplication_penalty,opportunity_score,status FROM seo_opportunities ORDER BY opportunity_score DESC LIMIT 20').all(),env.DB.prepare("SELECT substr(created_at,1,10) AS day,COUNT(*) AS clicks FROM click_events WHERE created_at >= datetime('now','-30 days') GROUP BY substr(created_at,1,10) ORDER BY day ASC").all(),env.DB.prepare("SELECT substr(created_at,1,10) AS day,COUNT(DISTINCT session_id) AS searches FROM search_events WHERE created_at >= datetime('now','-30 days') GROUP BY substr(created_at,1,10) ORDER BY day ASC").all(),readAffiliateMap(request,env)]);const tools=rowsOnly(byTool),intents=rowsOnly(byIntent),sources=rowsOnly(bySource),searchSources=rowsOnly(bySearchSource),searchRows=rowsOnly(searches),opps=rowsOnly(opportunities),clickDays=rowsOnly(dailyClicks),searchDays=rowsOnly(dailySearches);const activeSlugs=new Set(Object.entries(affiliate||{}).filter(([,entry])=>entry&&entry.enabled&&entry.url).map(([slug])=>slug));const monetizedClicks=tools.filter(row=>activeSlugs.has(String(row.tool_slug))).reduce((sum,row)=>sum+Number(row.clicks||0),0);const unmonetizedClicks=Number(total?.clicks||0)-monetizedClicks;const ownerClicks=sources.filter(r=>r.source==='internal-test').reduce((s,r)=>s+Number(r.clicks||0),0);const ownerSearches=searchSources.filter(r=>r.source==='internal-test').reduce((s,r)=>s+Number(r.searches||0),0);const otherClicks=Math.max(0,Number(total?.clicks||0)-ownerClicks);const otherSessions=await env.DB.prepare("SELECT COUNT(DISTINCT session_id) AS sessions FROM click_events WHERE source!='internal-test'").first();const ownerSessions=await env.DB.prepare("SELECT COUNT(DISTINCT session_id) AS sessions FROM click_events WHERE source='internal-test'").first();const affiliateCoverage={catalogTools:Object.keys(affiliate||{}).length,activeTools:activeSlugs.size,toolsWithClicks:tools.length,monetizedClicks,unmonetizedClicks,activeToolSlugs:[...activeSlugs]};const audience={ownerClicks,ownerSessions:Number(ownerSessions?.sessions||0),ownerSearches,otherClicks,otherSessions:Number(otherSessions?.sessions||0),classificationNote:'Only events explicitly marked internal-test are classified as owner activity. Older unmarked events remain in other/unclassified.'};return Response.json({total,byTool:tools,byIntent:intents,bySource:sources,bySearchSource:searchSources,searches:searchRows,opportunities:opps,dailyClicks:clickDays,dailySearches:searchDays,affiliateCoverage,audience},{headers:{'Content-Type':'application/json; charset=UTF-8','Cache-Control':'private, max-age=60'}});}
export default {async fetch(request,env,ctx){const url=new URL(request.url);const token=(request.headers.get('Authorization')||'').replace(/^Bearer\s+/i,'');const isPublicHost=url.hostname===new URL(BASE).hostname;const legacyAuthorized=!isPublicHost&&!!env.ADMIN_TOKEN&&token===env.ADMIN_TOKEN;const seoRedirect=await consolidatedSeoRedirect(request,env,url);if(seoRedirect)return seoRedirect;if(url.pathname==='/api/stats'&&request.method==='GET')return stats(request,env,ctx);if(url.pathname==='/api/content-signals'&&request.method==='GET'){if(isPublicHost||!legacyAuthorized)return Response.json({error:'unauthorized'},{status:401});try{return await contentSignals(request,env);}catch{return Response.json({ok:false,signals:[]},{status:500});}}if(url.pathname==='/api/click'&&request.method==='POST')return trackedClick(request,env);if(url.pathname==='/api/search'&&request.method==='POST')return trackedSearch(request,env);if(url.pathname.startsWith('/go/')){const tool=url.pathname.slice(4).toLowerCase().replace(/[^a-z0-9-]/g,'');const redirect=await trackedRedirect(request,env,tool);if(redirect)return redirect;}if(url.pathname==='/sitemap.xml'&&request.method==='GET'){try{return await dynamicSitemap(request,env);}catch{return base.fetch(request,env,ctx);}}if(url.pathname==='/robots.txt'&&request.method==='GET')return new Response(`User-agent: *\nAllow: /\n\nSitemap: ${BASE}/sitemap.xml\n`,{status:200,headers:{'Content-Type':'text/plain; charset=UTF-8','Cache-Control':'public, max-age=300, s-maxage=3600'}});if(url.pathname==='/analytics.html'&&isPublicHost)return new Response('Not found',{status:404,headers:{'Content-Type':'text/plain; charset=UTF-8','Cache-Control':'no-store'}});const slug=slugFromPath(url.pathname);if(slug){const staticResponse=await staticAsset(request,env);if(staticResponse)return staticResponse;const dynamicResponse=await dynamicOpportunity(request,env,slug);if(dynamicResponse)return dynamicResponse;}return base.fetch(request,env,ctx);},async scheduled(event,env,ctx){if(typeof base.scheduled==='function')return base.scheduled(event,env,ctx);}};
