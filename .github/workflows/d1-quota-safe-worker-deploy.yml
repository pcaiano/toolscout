name: D1 Quota Safe Worker Deploy

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/d1-quota-safe-worker-deploy.yml'

permissions:
  contents: read

concurrency:
  group: toolscout-worker-production
  cancel-in-progress: false

jobs:
  deploy-write-fix:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Validate D1 write controls
        run: |
          node --check distribution-network-worker.js
          node --check affiliate-coverage-cycle-worker.js
          node --check catalog-autonomy-worker.js
          node --check distribution-autonomous-worker.js
          node --check distribution-submission-worker.js
          node --check content-engine-intelligence-worker.js
          node --check command-center-light-theme-worker.js

      - name: Deploy Worker without D1 migrations
        run: npx wrangler deploy --config wrangler.toml --latest
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
