function json(data, status = 200) {
  return new Response(JSON.stringify(data), { status, headers: { 'content-type': 'application/json; charset=utf-8', 'cache-control': 'no-store' } });
}
function headers(variant, model = 'flux2') {
  return { 'content-type': 'image/jpeg', 'cache-control': 'public, max-age=86400', 'x-toolscout-renderer': 'flux2-brand-safe-v4', 'x-toolscout-image-variant': variant, 'x-toolscout-image-model': model };
}
function safe(value, max = 240) {
  return String(value || '').replace(/[\r\n]+/g,' ').replace(/[^\p{L}\p{N}\s.,:;!?()&+\-/'’]/gu,'').trim().slice(0,max);
}
function creativeFromUrl(url, variant) {
  return { variant, concept:safe(url.searchParams.get('concept')||variant,32), scene:safe(url.searchParams.get('scene')||'',420) };
}
function buildPrompt(c) {
  const idea=c.scene||c.concept;
  return `Create a premium square editorial campaign illustration for ToolScout, an independent software discovery platform.\n\nCENTRAL IDEA\n${idea}\n\nSTRICT TOOLSCOUT ART DIRECTION\nUse the visual language of ToolScout's product: restrained, precise, independent and useful. Contemporary editorial design with quiet charisma. Foundation colours are near-black charcoal #101828, soft off-white #F5F7FB and white, with restrained cool blue accents inspired by the product. Generous negative space, crisp geometry, subtle depth, refined materials, sophisticated B2B technology art direction. The visual must feel like one coherent brand system, not generic SaaS advertising.\n\nILLUSTRATION\nUse premium photorealistic CGI or high-end advertising photography to make the central idea immediately understandable. Prefer a single strong metaphor involving software selection, filtering, workflow, comparison, focus, decision-making or reducing choice. Realistic lighting and materials, elegant composition, restrained detail.\n\nABSOLUTE TEXT RULE\nTHE GENERATED ILLUSTRATION MUST CONTAIN NO WORDS, NO LETTERS, NO NUMBERS, NO LABELS, NO CAPTIONS, NO UI COPY, NO LOGOTYPES AND NO TYPOGRAPHY OF ANY KIND. Leave clean negative space so ToolScout typography can be added separately by a deterministic brand compositor. Do not attempt to spell ToolScout.\n\nAVOID\nNo random abstract lines or blobs. No cyberpunk neon, hologram overload, robots, stock-photo smiles, fake software interfaces, fake logos, fake charts, fake data, malformed icons, clip-art, Microsoft Paint aesthetics, distorted anatomy, illegible glyphs, watermarks or generic AI slop.\n\nFORMAT\n1024x1024. No border. No watermark. No text. Premium editorial campaign quality.`;
}
function decodeBase64Image(image){const binary=atob(image);const bytes=new Uint8Array(binary.length);for(let i=0;i<binary.length;i++)bytes[i]=binary.charCodeAt(i);return bytes;}
async function runFlux(env,model,prompt){const form=new FormData();form.append('prompt',prompt);form.append('width','1024');form.append('height','1024');form.append('guidance','4');const r=new Response(form);return env.AI.run(model,{multipart:{body:r.body,contentType:r.headers.get('content-type')}});}
async function generate(env,prompt){const attempts=['@cf/black-forest-labs/flux-2-klein-9b','@cf/black-forest-labs/flux-2-klein-4b'];const errors=[];for(const model of attempts){try{const result=await runFlux(env,model,prompt);if(result&&typeof result.image==='string'&&result.image.length>100)return{bytes:decodeBase64Image(result.image),model};errors.push(`${model}: empty image response`);}catch(e){errors.push(`${model}: ${e?.message||String(e)}`);}}throw new Error(errors.join(' | '));}
export default{async fetch(request,env){const url=new URL(request.url);const variant=safe(url.searchParams.get('variant')||'discovery',24).toLowerCase();if(url.pathname==='/health')return json({ok:true,service:'toolscout-social-image',renderer:'flux2-brand-safe-v4',primary:'@cf/black-forest-labs/flux-2-klein-9b',fallback:'@cf/black-forest-labs/flux-2-klein-4b',textPolicy:'no-generated-text'});if(url.pathname!=='/generate')return json({error:'not_found'},404);if(!['GET','HEAD'].includes(request.method))return json({error:'method_not_allowed'},405);if(request.method==='HEAD')return new Response(null,{status:200,headers:headers(variant)});try{const creative=creativeFromUrl(url,variant);const generated=await generate(env,buildPrompt(creative));return new Response(generated.bytes,{status:200,headers:headers(variant,generated.model)});}catch(e){return json({error:'image_generation_failed',detail:e?.message||String(e)},503);}}};