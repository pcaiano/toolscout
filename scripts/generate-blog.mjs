import fs from 'node:fs';
import path from 'node:path';

const ROOT=process.cwd();
const BASE='https://toolscout.luxurybuyerintelligence.workers.dev';
const SIGNALS=`${BASE}/api/content-signals`;
const intents=JSON.parse(fs.readFileSync(path.join(ROOT,'data','intents.json'),'utf8'));
const ACRONYMS=new Map([['ai','AI'],['crm','CRM'],['seo','SEO'],['api','API'],['saas','SaaS'],['roi','ROI']]);
const words=slug=>String(slug||'').replace(/^best-/,'').replace(/-/g,' ').trim();
const title=slug=>words(slug).split(/\s+/).map(w=>ACRONYMS.get(w.toLowerCase())||w.charAt(0).toUpperCase()+w.slice(1)).join(' ');
const esc=value=>String(value??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#39;');

const sleep=ms=>new Promise(resolve=>setTimeout(resolve,ms));
async function getSignals(){
  for(let attempt=0;attempt<5;attempt++){
    try{const r=await fetch(SIGNALS,{headers:{Accept:'application/json'}});if(r.ok){const j=await r.json();if(Array.isArray(j.signals))return j.signals;}}catch{}
    if(attempt<4)await sleep(4000*(attempt+1));
  }
  return[];
}

const article=topic=>{const guideUrl=`${BASE}/${topic.intent}.html`;return `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${esc(topic.title)} — ToolScout Blog</title><meta name="description" content="${esc(topic.description)}"><link rel="canonical" href="${BASE}/blog/${encodeURIComponent(topic.slug)}.html"><meta name="robots" content="index,follow"><meta property="og:title" content="${esc(topic.title)}"><meta property="og:description" content="${esc(topic.description)}"><style>body{font-family:Inter,system-ui,-apple-system,sans-serif;margin:0;background:#f7f8fa;color:#111827}.wrap{max-width:900px;margin:auto;padding:28px 22px 80px}.brand{font-size:22px;font-weight:850;color:#111827;text-decoration:none}.hero{padding:72px 0 38px}.eyebrow{font-size:11px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:#667085}.hero h1{font-size:clamp(42px,7vw,68px);line-height:1.02;letter-spacing:-.055em;margin:14px 0 18px}.dek{font-size:20px;line-height:1.65;color:#667085}.article{font-size:18px;line-height:1.75;color:#344054}.article h2{font-size:30px;letter-spacing:-.035em;margin:42px 0 12px;color:#111827}.box{background:#fff;border:1px solid #e4e7ec;border-radius:18px;padding:20px;margin:24px 0}.cta{display:inline-block;background:#111827;color:#fff;text-decoration:none;padding:12px 16px;border-radius:10px;font-weight:750}.meta{font-size:12px;color:#667085;margin-top:28px}.related{margin-top:46px;padding-top:28px;border-top:1px solid #e4e7ec}.related a{display:inline-block;margin:8px 10px 0 0;color:#111827;text-decoration:none;border-bottom:1px solid #cbd1d9;padding-bottom:3px}</style></head><body><div class="wrap"><a class="brand" href="/">ToolScout</a><main class="hero"><div class="eyebrow">ToolScout Blog</div><h1>${esc(topic.title)}</h1><p class="dek">${esc(topic.description)}</p></main><article class="article"><p>People looking for <strong>${esc(words(topic.intent))}</strong> usually need help deciding what matters, which trade-offs are real, and which tools deserve a closer look.</p><h2>Start with the job</h2><p>Define the outcome first. The right tool depends on team size, workflow, budget, integrations and the amount of automation you actually need.</p><h2>Compare the criteria that matter</h2><p>Focus on the capabilities connected to the job rather than accumulating features. Check ease of use, pricing, implementation effort and whether the product fits the way the team already works.</p><div class="box"><strong>See the current ToolScout shortlist</strong><p>The live recommendation page compares the available catalog against this intent and keeps the decision centred on practical fit.</p><a class="cta" href="${guideUrl}">Compare tools →</a></div><h2>Watch the trade-offs</h2><p>A less expensive tool may take more setup. A powerful platform may be more complexity than a small team needs. The strongest choice is the one with the fewest costly compromises for the actual workflow.</p><h2>Keep the shortlist small</h2><p>Three credible options are usually easier to evaluate than twenty loosely related products. Compare them against the same criteria and test the one closest to the job.</p><div class="related"><strong>Continue with ToolScout</strong><br><a href="/blog/">Browse the blog</a><a href="/guides.html">Browse all guides</a><a href="${guideUrl}">Open this recommendation</a><a href="/">Describe your needs</a></div></article><div class="meta">This topic was prioritised from aggregated ToolScout search activity. ToolScout may earn affiliate compensation from some outbound links.</div></div></body></html>`;};

const signals=await getSignals();
const signalMap=new Map(signals.map(x=>[String(x.intent_slug),Number(x.searches||0)]));
const byIntent=new Map(intents.filter(x=>x?.slug).map(x=>[x.slug,x]));
const discovered=signals.filter(x=>x?.intent_slug&&!byIntent.has(String(x.intent_slug))).map(x=>({slug:String(x.intent_slug),title:title(x.intent_slug),category:'discovered',searches:Number(x.searches||0)}));
const catalog=intents.filter(x=>x?.slug).map(x=>({slug:x.slug,title:title(x.slug),category:x.category||'software',searches:signalMap.get(x.slug)||0}));
const ranked=[...catalog,...discovered].sort((a,b)=>b.searches-a.searches||a.title.localeCompare(b.title));
const topics=ranked.slice(0,50).map(item=>({slug:`${item.slug}-guide`,intent:item.slug,title:`${item.title}: How to Choose the Right Tool`,description:`A practical guide to choosing software for ${words(item.slug)}, shaped by ToolScout demand signals and the criteria that matter.`,searches:item.searches}));

const dir=path.join(ROOT,'blog');fs.mkdirSync(dir,{recursive:true});let created=0,skipped=0;
for(const topic of topics){const file=path.join(dir,`${topic.slug}.html`);if(fs.existsSync(file)){skipped++;continue;}fs.writeFileSync(file,article(topic),'utf8');created++;}
const cards=topics.map(t=>`<a class="card" href="./${encodeURIComponent(t.slug)}.html"><strong>${esc(t.title)}</strong><span>${t.searches?`${t.searches} recent search session${t.searches===1?'':'s'} · `:''}Read article →</span></a>`).join('');
const index=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>ToolScout Blog — Software buying guides</title><meta name="description" content="Practical software buying guides prioritised from the needs people explore on ToolScout."><link rel="canonical" href="${BASE}/blog/"><meta name="robots" content="index,follow"><style>body{font-family:Inter,system-ui,-apple-system,sans-serif;margin:0;background:#f7f8fa;color:#111827}.wrap{max-width:1000px;margin:auto;padding:28px 22px 80px}.brand{font-size:22px;font-weight:850;color:#111827;text-decoration:none}.hero{padding:68px 0 30px}.eyebrow{font-size:11px;font-weight:800;letter-spacing:.14em;text-transform:uppercase;color:#667085}.hero h1{font-size:clamp(42px,7vw,68px);line-height:1;letter-spacing:-.055em;margin:14px 0}.hero p{font-size:19px;line-height:1.65;color:#667085;max-width:760px}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.card{display:block;background:#fff;border:1px solid #e4e7ec;border-radius:16px;padding:20px;text-decoration:none;color:#111827}.card strong{display:block;font-size:17px;line-height:1.35}.card span{display:block;margin-top:10px;font-size:12px;color:#667085}@media(max-width:700px){.grid{grid-template-columns:1fr}}</style></head><body><div class="wrap"><a class="brand" href="${BASE}/">ToolScout</a><main class="hero"><div class="eyebrow">ToolScout Blog</div><h1>Better software decisions start with the job.</h1><p>Practical buying guides prioritised from the needs people explore on ToolScout — not generic content calendars.</p></main><section class="grid">${cards}</section></div></body></html>`;
fs.writeFileSync(path.join(dir,'index.html'),index,'utf8');
fs.mkdirSync(path.join(ROOT,'reports'),{recursive:true});fs.writeFileSync(path.join(ROOT,'reports','blog-topics.json'),JSON.stringify({generatedAt:new Date().toISOString(),source:'aggregated content demand signals',count:topics.length,items:topics},null,2)+'\n');
console.log(JSON.stringify({topics:topics.length,created,skipped,signals:signals.length}));
