import { execFileSync } from 'node:child_process';
import { mkdirSync, writeFileSync, readFileSync, existsSync } from 'node:fs';
function q(sql){const out=execFileSync('npx',['wrangler','d1','execute','toolscout','--remote','--config','wrangler.toml','--json','--command',sql],{encoding:'utf8',stdio:['ignore','pipe','inherit']});const p=JSON.parse(out),b=Array.isArray(p)?p[0]:p;return b?.results||b?.result?.[0]?.results||[];}
const n=v=>Number(v||0), pct=(a,b)=>b?Number((a/b*100).toFixed(1)):null;
const startWindow={today:"date(st.created_at,'+1 hour')=date('now','+1 hour')",d7:"st.created_at>=datetime('now','-7 days')",d30:"st.created_at>=datetime('now','-30 days')"};
const traffic={};
for(const [k,w] of Object.entries(startWindow)){
  const r=q(`SELECT COUNT(DISTINCT CASE WHEN s.classification='likely-human' THEN st.session_id END) likely_human,COUNT(DISTINCT CASE WHEN s.classification='owner' THEN st.session_id END) owner FROM funnel_events st JOIN sessions s ON s.session_id=st.session_id WHERE st.event_type='session_started' AND ${w};`)[0]||{};
  const raw=q(`SELECT COUNT(*) total,SUM(classification='likely-human') likely_human,SUM(classification='known-bot/crawler') bots,SUM(classification='owner') owner,SUM(classification='synthetic/test') synthetic,SUM(classification='unknown/legacy') legacy FROM sessions WHERE ${k==='today'?"date(first_seen_at,'+1 hour')=date('now','+1 hour')":k==='d7'?"first_seen_at>=datetime('now','-7 days')":"first_seen_at>=datetime('now','-30 days')"};`)[0]||{};
  traffic[k]={total:n(r.likely_human)+n(r.owner),likelyHuman:n(r.likely_human),owner:n(r.owner),bots:0,synthetic:0,unknownLegacy:0,rawSessions:{total:n(raw.total),likelyHuman:n(raw.likely_human),bots:n(raw.bots),owner:n(raw.owner),synthetic:n(raw.synthetic),unknownLegacy:n(raw.legacy)}};
}
const daily=q(`SELECT date(st.created_at,'+1 hour') day,COUNT(DISTINCT st.session_id) sessions FROM funnel_events st JOIN sessions s ON s.session_id=st.session_id WHERE st.event_type='session_started' AND s.classification='likely-human' AND st.created_at>=datetime('now','-30 days') GROUP BY day ORDER BY day;`).map(r=>({day:r.day,sessions:n(r.sessions)}));
const validHumanEvent=`s.classification='likely-human' AND EXISTS (SELECT 1 FROM funnel_events start WHERE start.session_id=f.session_id AND start.event_type='session_started' AND start.created_at<=f.created_at)`;
const funnelRows=q(`SELECT f.event_type,COUNT(*) events,COUNT(DISTINCT f.session_id) sessions FROM funnel_events f JOIN sessions s ON s.session_id=f.session_id WHERE ${validHumanEvent} AND f.created_at>=datetime('now','-30 days') GROUP BY f.event_type;`);const funnel=Object.fromEntries(funnelRows.map(r=>[r.event_type,{events:n(r.events),sessions:n(r.sessions)}]));
const clicks=q(`SELECT COUNT(*) clicks,COUNT(DISTINCT c.session_id) sessions,SUM(CASE WHEN c.affiliate_active_at_click=1 THEN 1 ELSE 0 END) monetized FROM click_events c JOIN sessions s ON s.session_id=c.session_id WHERE s.classification='likely-human' AND c.created_at>=datetime('now','-30 days') AND c.source!='internal-test' AND EXISTS (SELECT 1 FROM funnel_events start WHERE start.session_id=c.session_id AND start.event_type='session_started' AND start.created_at<=c.created_at);`)[0]||{};
const clickStats={outboundClicks:n(clicks.clicks),sessionsWithOutbound:n(clicks.sessions),monetizedOutbound:n(clicks.monetized),monetizationCoverage:pct(n(clicks.monetized),n(clicks.clicks)),humanSessionToOutboundRate:pct(n(clicks.sessions),traffic.d30.likelyHuman)};
const sources=q(`SELECT COALESCE(NULLIF(st.source,''),'direct') source,COUNT(DISTINCT st.session_id) sessions FROM funnel_events st JOIN sessions s ON s.session_id=st.session_id WHERE s.classification='likely-human' AND st.event_type='session_started' AND st.created_at>=datetime('now','-30 days') GROUP BY COALESCE(NULLIF(st.source,''),'direct') ORDER BY sessions DESC LIMIT 20;`).map(r=>({source:r.source,sessions:n(r.sessions)}));
const intents=q(`SELECT COALESCE(f.intent_slug,'unlabelled') intent,COUNT(DISTINCT f.session_id) sessions,SUM(CASE WHEN f.event_type='recommendation_completed' THEN 1 ELSE 0 END) recommendations,SUM(CASE WHEN f.event_type='outbound_clicked' THEN 1 ELSE 0 END) outbound FROM funnel_events f JOIN sessions s ON s.session_id=f.session_id WHERE ${validHumanEvent} AND f.created_at>=datetime('now','-30 days') GROUP BY COALESCE(f.intent_slug,'unlabelled') ORDER BY outbound DESC,sessions DESC LIMIT 25;`).map(r=>({intent:r.intent,sessions:n(r.sessions),recommendationCompletions:n(r.recommendations),outboundClicks:n(r.outbound)}));
const revenue=q(`SELECT currency,status,attribution_status,COUNT(*) conversions,SUM(commission) commission,SUM(amount) amount FROM revenue_ledger WHERE created_at>=datetime('now','-90 days') GROUP BY currency,status,attribution_status ORDER BY currency,status;`).map(r=>({currency:r.currency,status:r.status,attributionStatus:r.attribution_status,conversions:n(r.conversions),commission:n(r.commission),amount:n(r.amount)}));
const verifiedRevenue=revenue.filter(x=>x.attributionStatus==='vendor_confirmed'&&['confirmed','paid'].includes(x.status));
const forensic={
  definition:'Traffic KPIs count only likely-human sessions with an observed public page-entry event. Redirect-only sessions are excluded from traffic and funnel conversion metrics.',
  sessionStartsToday:q(`SELECT COALESCE(NULLIF(f.source,''),'direct') source,COALESCE(NULLIF(f.referrer_host,''),'none') referrer_host,COALESCE(NULLIF(f.path,''),'none') path,COUNT(DISTINCT f.session_id) sessions FROM funnel_events f JOIN sessions s ON s.session_id=f.session_id WHERE s.classification='likely-human' AND f.event_type='session_started' AND date(f.created_at,'+1 hour')=date('now','+1 hour') GROUP BY COALESCE(NULLIF(f.source,''),'direct'),COALESCE(NULLIF(f.referrer_host,''),'none'),COALESCE(NULLIF(f.path,''),'none') ORDER BY sessions DESC;`).map(r=>({source:r.source,referrerHost:r.referrer_host,path:r.path,sessions:n(r.sessions)})),
  redirectOnlyToday:n(q(`SELECT COUNT(*) sessions FROM sessions s WHERE s.classification='likely-human' AND date(s.first_seen_at,'+1 hour')=date('now','+1 hour') AND NOT EXISTS (SELECT 1 FROM funnel_events f WHERE f.session_id=s.session_id AND f.event_type='session_started');`)[0]?.sessions),
  redirectOnly30d:n(q(`SELECT COUNT(*) sessions FROM sessions s WHERE s.classification='likely-human' AND s.first_seen_at>=datetime('now','-30 days') AND NOT EXISTS (SELECT 1 FROM funnel_events f WHERE f.session_id=s.session_id AND f.event_type='session_started');`)[0]?.sessions),
  excludedRedirectClicks30d:n(q(`SELECT COUNT(*) clicks FROM click_events c JOIN sessions s ON s.session_id=c.session_id WHERE s.classification='likely-human' AND c.created_at>=datetime('now','-30 days') AND NOT EXISTS (SELECT 1 FROM funnel_events start WHERE start.session_id=c.session_id AND start.event_type='session_started' AND start.created_at<=c.created_at);`)[0]?.clicks)
};
const snapshot={generatedAt:new Date().toISOString(),timezone:'Europe/Lisbon',definition:'Growth and funnel KPIs use observed likely-human public page entries. Redirect-only sessions are excluded. Revenue is reported only from revenue_ledger evidence and is never inferred from clicks.',traffic:{...traffic,dailyLikelyHuman30d:daily,averageDailyLikelyHuman30d:daily.length?Number((daily.reduce((s,x)=>s+x.sessions,0)/30).toFixed(2)):0},funnel30d:{recommendationStarted:funnel.recommendation_started||{events:0,sessions:0},recommendationCompleted:funnel.recommendation_completed||{events:0,sessions:0},resultViewed:funnel.recommendation_result_viewed||{events:0,sessions:0},toolViewed:funnel.tool_viewed||{events:0,sessions:0},outboundClicked:funnel.outbound_clicked||{events:0,sessions:0},recommendationCompletionRate:pct(n(funnel.recommendation_completed?.sessions),n(funnel.recommendation_started?.sessions))},commercial30d:clickStats,sources30d:sources,intents30d:intents,forensic,revenue90d:{ledger:revenue,verified:verifiedRevenue,knownRevenueStatus:verifiedRevenue.length?'observed':'no-vendor-confirmed-revenue-recorded'},projectionInputs:{humanSessions30d:traffic.d30.likelyHuman,humanDailyAverage30d:daily.length?Number((daily.reduce((s,x)=>s+x.sessions,0)/30).toFixed(2)):0,recommendationCompletionRate:pct(n(funnel.recommendation_completed?.sessions),traffic.d30.likelyHuman),humanSessionToOutboundRate:clickStats.humanSessionToOutboundRate,monetizationCoverage:clickStats.monetizationCoverage,note:'Observed page-entry sessions only. Historical static-page landings that never emitted a session_started event cannot be reconstructed from D1.'}};
mkdirSync('data',{recursive:true});writeFileSync('data/business-intelligence.json',JSON.stringify(snapshot,null,2)+'\n');const hp='data/business-intelligence-history.json';let h=[];if(existsSync(hp)){try{h=JSON.parse(readFileSync(hp,'utf8'));}catch{h=[];}}if(!Array.isArray(h))h=[];h.push(snapshot);h=h.slice(-90);writeFileSync(hp,JSON.stringify(h,null,2)+'\n');console.log(JSON.stringify(snapshot));